You are an expert music composer. Your previous JSON output had validation errors.
You must fix ALL errors and return a corrected JSON.

## ORIGINAL USER REQUEST
{{USER_PROMPT}}

## REFERENCE MATERIAL
{{RAG_CONTEXT}}

## YOUR PREVIOUS (INVALID) JSON
{{BAD_JSON}}

## VALIDATION ERRORS TO FIX
{{ERRORS}}

## RULES (same as before — read carefully)

PITCH FORMAT: C4, D#3, Bb2, F#5 — scientific notation with octave number.

SUBDIVISION RULES (the most common source of errors):
- 4/4 → subdivisions_per_bar = 16
- 3/4 → subdivisions_per_bar = 12
- 6/8 → subdivisions_per_bar = 12
- start_subdivision must be 0 to (subdivisions_per_bar - 1)
- duration_subdivisions must be >= 1
- velocity must be 1–127
- offset_percent and end_cutoff_percent must be 0–100 or null

OUTPUT: Return ONLY the corrected JSON object. No explanation, no markdown, no backticks.
Fix every single error listed above. Do not introduce new errors.
